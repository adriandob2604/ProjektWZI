import React, { useEffect, useState } from "react";
interface createStation {
  name?: string;
  adres?: string;
  price?: number;
  actual_date?: string;
}

// function addStations(): JSX.Element {
//     const [station, setStation] = useState<createStation>({})

// }
